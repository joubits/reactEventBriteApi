import React from 'react'
//import PropTypes from 'prop-types'

const Header = () => (
    <header className="uk-margin" uk-margin="true">
        <h1 className="uk-text-center">Events with EvenBrite API</h1>
    </header>
);

// header.propTypes = {

// }

export default Header
